#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>


int monthcounter()
{
    int A;
    int B;
    int C;
    int D;
    int bulan = 0;
    int makan = 0;

    while (A > 0 || B > 0 || C > 0 || D > 0)
    {
        if (A > 0 || B > 0)
        {
            if (A > 0)
            {
                int eatA = A % 3;

                if (eatA == 0)
                {
                    makan == 3;
                }
                
            }
            
        }
        
    }
    

}